---
alwaysApply: false
description: SQLite library_folders and documents.folder_id schema design notes
---

## library_folders 表设计

- `library_folders` 用来存储每个 `library` 下的文件夹信息，包含：
  - `id`, `created_at`, `updated_at`
  - `library_id`：所属 library
  - `name`：文件夹名称
  - `sort_order`：排序字段（整型，默认 0）
  - `parent_id`：父文件夹 ID，用于支持多级嵌套文件夹

- 索引和唯一约束：
  - `idx_folders_library_sort`：`(library_id, sort_order)`，用于同一库内按排序高效获取文件夹列表。
  - `idx_folders_parent_id`：`(parent_id)`，用于按父文件夹查子节点。
  - `idx_folders_library_parent_name`：`(library_id, COALESCE(parent_id, -1), name)` 唯一索引。
    - 允许同一 `library` 内不同父文件夹下存在同名文件夹。
    - 在同一父文件夹下（`parent_id` 相同或同为 `NULL`）不允许重名。

## documents.folder_id 字段设计

- 在 `documents` 表中新增 `folder_id integer` 字段，用于将文档归类到 `library_folders`。
- 目前迁移中仅创建普通整型列和索引 `idx_docs_folder_id`：
  - `CREATE INDEX IF NOT EXISTS idx_docs_folder_id ON documents(folder_id);`
- 外键约束 `folder_id` → `library_folders.id` 且 `ON DELETE SET NULL` 未直接通过 SQLite 约束实现：
  - SQLite 对 `ALTER TABLE ADD CONSTRAINT` 支持有限，复杂外键迁移可能需要重建表。
  - 若需要严格保证「删除文件夹时自动将相关文档的 `folder_id` 置为 `NULL`」，请在应用层或后续数据迁移中处理该逻辑。

